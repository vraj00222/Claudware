# demo-fixtures/

Cached runs for the wifi-death drill (cache at h18). For each of the 3 demo
objects: PNG renders, STL, G-code. mockStream replays these identically if
wifi / API / printer dies during the demo. Also keep the physically-printed
hybrid figure ready in hand.
